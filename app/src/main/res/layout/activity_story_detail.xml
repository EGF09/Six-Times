<?xml version="1.0" encoding="utf-8"?>
<!-- Hikaye Detay ve Oluşturma Ekranı Layout Dosyası -->
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#08080C">

    <!-- Üst Katman: Geri Butonu -->
    <ImageButton
        android:id="@+id/btnBack"
        android:layout_width="44dp"
        android:layout_height="44dp"
        android:layout_margin="16dp"
        android:background="@drawable/bg_neon_back"
        android:src="@android:drawable/ic_menu_revert"
        app:tint="#00F0FF"
        android:contentDescription="Geri Dön"
        android:elevation="4dp"
        android:layout_alignParentTop="true"
        android:layout_alignParentStart="true"
        android:translationZ="10dp"/>

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scrollbars="none">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <!-- Hikaye Görseli -->
            <ImageView
                android:id="@+id/ivFullStoryImage"
                android:layout_width="match_parent"
                android:layout_height="300dp"
                android:scaleType="centerCrop"
                android:src="@android:drawable/ic_menu_gallery" />

            <!-- Hikaye İçerik Kartı -->
            <androidx.cardview.widget.CardView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_margin="16dp"
                app:cardCornerRadius="16dp"
                app:cardElevation="0dp">

                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:background="@drawable/bg_neon_card"
                    android:orientation="vertical"
                    android:padding="20dp">

                    <!-- Başlık ve Talimatlar -->
                    <TextView
                        android:id="@+id/tvStoryTitle"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="Yeni Hikaye Oluştur"
                        android:textColor="#FFFFFF"
                        android:textSize="24sp"
                        android:textStyle="bold"
                        android:layout_marginBottom="8dp"/>

                    <TextView
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="Hikayede kullanılacak kelimeleri seçin:"
                        android:textColor="#888899"
                        android:textSize="14sp"
                        android:layout_marginBottom="12dp"/>

                    <!-- Dinamik Kelime Seçim Alanı -->
                    <com.google.android.material.chip.ChipGroup
                        android:id="@+id/chipGroupWords"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:layout_marginBottom="16dp"
                        app:singleSelection="false">
                    </com.google.android.material.chip.ChipGroup>

                    <!-- Oluşturma Butonu -->
                    <com.google.android.material.button.MaterialButton
                        android:id="@+id/btnGenerate"
                        android:layout_width="match_parent"
                        android:layout_height="56dp"
                        android:text="Hikaye ve Görsel Oluştur"
                        android:textAllCaps="false"
                        android:textColor="#08080C"
                        android:textStyle="bold"
                        app:backgroundTint="#00F0FF"
                        app:cornerRadius="12dp"
                        android:layout_marginBottom="24dp"/>

                    <!-- Yükleme Göstergesi -->
                    <ProgressBar
                        android:id="@+id/progressBar"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:layout_gravity="center"
                        android:visibility="gone"
                        android:layout_marginBottom="16dp"/>

                    <!-- Oluşturulan Hikaye Metni -->
                    <TextView
                        android:id="@+id/tvFullStoryText"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:text=""
                        android:textColor="#FFFFFF"
                        android:textSize="16sp"
                        android:lineSpacingExtra="6dp"
                        android:layout_marginBottom="24dp"/>

                    <!-- Kaydetme Butonu -->
                    <com.google.android.material.button.MaterialButton
                        android:id="@+id/btnSave"
                        android:layout_width="match_parent"
                        android:layout_height="56dp"
                        android:text="Kaydet"
                        android:textAllCaps="false"
                        android:textColor="#FFFFFF"
                        android:textStyle="bold"
                        app:backgroundTint="#FF007A"
                        app:cornerRadius="12dp"
                        android:visibility="gone"
                        android:layout_marginBottom="24dp"/>

                </LinearLayout>
            </androidx.cardview.widget.CardView>
        </LinearLayout>
    </ScrollView>
</RelativeLayout>